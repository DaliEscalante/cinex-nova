export const API_LARAVEL = import.meta.env.VITE_LARAVEL_API || "";
export const API_NODE = import.meta.env.VITE_NODE_API || "";
export const USE_REMOTE = false;

// Cuando conectes el backend, cambia USE_REMOTE = true y configura las URLs
